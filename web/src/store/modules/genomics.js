const state = {
  refSeq: {
    url: "https://www.ncbi.nlm.nih.gov/nuccore/NC_045512.2/",
    name: "NC_045512"
  }
}

// getters
const getters = {};

// actions
const actions = {};

// mutations
const mutations = {};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
};
